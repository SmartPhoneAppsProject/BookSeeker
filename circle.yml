machine:
  node:
    version: 6.9
dependencies:
  override:
    - yarn
  pre:
    - yarn global add exp@48.0.4
    - echo 'API_ENDPOINT=test' > .env
  test:
    override:
      - echo "no tests"
deployment:
  appr:
    branch: /.*/
    commands:
      - 'if [ "$CI_PULL_REQUEST" != "" ]; then yarn appr; fi'